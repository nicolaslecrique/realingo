import 'package:flutter/material.dart';

class LessonSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Dialog(
        child: Center(
      child: Container(
        child: Text("Hello !"),
      ),
    ));
  }
}
